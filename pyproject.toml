[build-system]
requires = ["setuptools", "setuptools-rust"]
build-backend = "setuptools.build_meta"


[project]
name = "loxmqttrelay"
version = "0.4.0"
description = "MQTT Relay for Loxone"
requires-python = ">=3.14"
# The Miniserver websocket and its crypto are Rust now (the `loxwebsocket`
# crate), so neither the Python client nor pycryptodome is a dependency any
# more. What is left is the cold path: config, the whitelist sync, logging.
dependencies = [
    "aiohttp>=3.14.1",
    "orjson>=3.11.9",
    "uvloop>=0.22.1",
    "tomlkit>=0.15.0",
    "lxml>=6.1.1",
    "lz4>=4.4.5",
    "pygixml>=0.11.0",
]

[project.optional-dependencies]
test = [
    "pytest>=9.0.0",
    "pytest-asyncio>=1.4.0",
    "pytest-cov>=7.1.0"
]
dev = [
    "pytest>=9.0.0",
    "pytest-asyncio>=1.4.0",
    "pytest-cov>=7.1.0",
    "pyinstrument>=5.1.2",
    "setuptools>=82.0.1", 
    "setuptools-rust>=1.12.1"
]
build = [
    "setuptools>=82.0.1",
    "setuptools-rust>=1.12.1"
]

[tool.uv]
system-certs = true

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["loxmqttrelay"]

[project.scripts]
loxmqttrelay = "loxmqttrelay.main:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-v --cov=loxmqttrelay --cov-report=term-missing"
asyncio_mode = "strict"
asyncio_default_fixture_loop_scope = "function"
