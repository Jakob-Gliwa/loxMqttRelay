"""Developer harness for driving the relay against a Miniserver that is not there.

Import from :mod:`tests.harness.mock_miniserver`; nothing is re-exported here,
because the module and the context manager inside it share a name.
"""
